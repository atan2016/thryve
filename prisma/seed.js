console.log("Seed placeholder: the app ships with an in-memory demo dataset for local development.");
console.log("Run `prisma db push` after configuring DATABASE_URL to create the PostgreSQL schema.");
